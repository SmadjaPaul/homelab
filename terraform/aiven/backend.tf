# Backend configuration for remote state (optional - uncomment if using remote state)
# terraform {
#   backend "s3" {
#     bucket = "your-terraform-state-bucket"
#     key    = "aiven/terraform.tfstate"
#     region = "us-east-1"
#   }
# }
