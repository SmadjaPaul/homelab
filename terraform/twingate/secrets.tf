# Twingate secrets from Doppler
# These are accessed via data.doppler_secrets.infrastructure.map.TWINGATE_API_TOKEN
# and used in the provider configuration

# Available secrets in Doppler project "infrastructure":
# - TWINGATE_API_TOKEN
