# LiteLLM credentials (API keys for Synthetic, OpenAI, etc.)
# Add resources when secrets are ready. Example:
#
# resource "litellm_credential" "synthetic" {
#   key_name = "synthetic"
#   key_value = var.synthetic_api_key   # from Vault / env
# }
#
# See: https://registry.terraform.io/providers/ncecere/litellm/latest/docs/resources/credential
