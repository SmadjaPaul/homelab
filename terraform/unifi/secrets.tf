# UniFi secrets from Doppler
# These are accessed via data.doppler_secrets.infrastructure.map.UNIFI_PASSWORD
# and used in the provider configuration

# Available secrets in Doppler project "infrastructure":
# - UNIFI_PASSWORD
# - UNIFI_USERNAME (optional, defaults to "terraform")
