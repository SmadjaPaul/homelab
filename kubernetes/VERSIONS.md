# Versions Centralisees
# Ce fichier documente toutes les versions utilisees dans le cluster

# =============================================================================
# INFRASTRUCTURE
# =============================================================================

traefik:
  chart: "39.0.2"
  app: "3.6.7"

cloudflared:
  chart: "1.8.0"
  app: "2026.2.0"

external-dns:
  chart: "1.20.0"
  app: "0.20.0"

external-secrets:
  chart: "2.0.1"
  app: "2.0.1"

cert-manager:
  chart: "1.19.3"
  app: "1.19.4"

# =============================================================================
# STORAGE
# =============================================================================

cloudnative-pg:
  chart: "0.27.1"
  app: "1.27.1"

redis:
  chart: "20.6.0"
  app: "7.4.1"

# =============================================================================
# SECURITY
# =============================================================================

authentik:
  chart: "2025.12.4"
  app: "2025.12.4"

vaultwarden:
  chart: "3.2.1"
  app: "1.33.2"

kyverno:
  chart: "3.7.1"
  app: "1.17.1"

# =============================================================================
# MEDIA
# =============================================================================

audiobookshelf:
  chart: "3.2.1"
  app: "2.12.1"

immich:
  chart: "0.10.3"
  app: "2.0.0"

navidrome:
  chart: "0.14.1"
  app: "0.53.1"

lidarr:
  chart: "5.6.1"
  app: "2.5.1"

# =============================================================================
# AUTOMATION
# =============================================================================

n8n:
  chart: "0.28.1"
  app: "1.92.2"

# =============================================================================
# BUSINESS
# =============================================================================

outline:
  chart: "1.8.1"
  app: "0.77.0"

vikunja:
  chart: "0.23.1"
  app: "0.24.2"

paperless-ngx:
  chart: "5.1.0"
  app: "2.2.1"

umami:
  chart: "3.1.0"
  app: "2.17.0"

# =============================================================================
# NETWORK
# =============================================================================

netbird:
  chart: "0.31.1"
  app: "0.30.0"

# =============================================================================
# OBSERVABILITY
# =============================================================================

k8s-monitoring:
  chart: "2.1.0"
  app: "0.9.1"

# =============================================================================
# FLUX
# =============================================================================

flux:
  version: "v2.8.0"

# =============================================================================
# NOTES
# =============================================================================

# Mise a jour des versions:
# 1. Mettre a jour ce fichier
# 2. Mettre a jour les HelmReleases correspondants
# 3. Tester en staging (quand disponible)
# 4. Appliquer en production
#
# Cycle de mise a jour recommande:
# - Hebdomadaire: security patches
# - Mensuel: mises a jour mineures
# - Trimestriel: mises a jour majeures
