# SSH key no longer needed - repo is public
# Keeping the file as documentation but it's not applied
# apiVersion: external-secrets.io/v1
# kind: ExternalSecret
# metadata:
#   name: flux-system
#   namespace: flux-system
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: doppler
#   target:
#     name: flux-system
#     creationPolicy: Owner
#   data:
#     - secretKey: FLUX_GIT_SSH_KEY
#       remoteRef:
#         key: FLUX_GIT_SSH_KEY
