apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fleetdm
  namespace: business
spec:
  interval: 1h
  chart:
    spec:
      chart: fleet
      version: "0.1.0" # Placeholder, review chart source
      sourceRef:
        kind: HelmRepository
        name: fleetdm
        namespace: flux-system
  values:
    # Requires MySQL and Redis
    image:
      repository: fleetdm/fleet
      tag: v4.41.0
