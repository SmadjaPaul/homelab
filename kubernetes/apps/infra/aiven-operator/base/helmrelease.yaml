apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: aiven-operator
  namespace: aiven-system
spec:
  releaseName: aiven-operator
  targetNamespace: aiven-system
  interval: 1h
  timeout: 5m
  dependsOn:
    - name: external-secrets
      namespace: external-secrets
  chart:
    spec:
      chart: aiven-operator
      version: "0.19.0"
      sourceRef:
        kind: HelmRepository
        name: aiven-operator
        namespace: flux-system
  values:
    replicaCount: 1
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        memory: 256Mi
  install:
    createNamespace: true
    remediation:
      retries: 3
      remediateLastFailure: true
  upgrade:
    force: false
    remediation:
      retries: 3
      remediateLastFailure: true
  driftDetection:
    mode: enabled
