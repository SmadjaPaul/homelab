# Root Kustomization - All applications
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Infrastructure (deployed first via dependsOn)
  - ./infra
  # Public services
  - ./public/homepage/base
