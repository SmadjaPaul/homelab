apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # Grafana k8s-monitoring (Alloy) — metrics, logs, events → Grafana Cloud
  - k8s-monitoring/base
  # Grafana for visualization
  - grafana/base
