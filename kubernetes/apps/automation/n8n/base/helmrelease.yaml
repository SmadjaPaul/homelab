apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: n8n
  namespace: automation
spec:
  interval: 1h
  releaseName: n8n
  chart:
    spec:
      chart: n8n
      version: "1.16.29"
      sourceRef:
        kind: HelmRepository
        name: community-charts
        namespace: automation
  values:
    # Trying extremely flat structure
    image:
      repository: n8nio/n8n
    service:
      port: 5678
    # If this fails, the schema is likely broken or I am misreading it.
