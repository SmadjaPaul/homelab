# Comet Environment Variables
# These are placeholder values - replace with actual values or use Doppler/External Secrets
apiVersion: v1
kind: Secret
metadata:
  name: comet-env
  namespace: media
type: Opaque
stringData:
  # Comet configuration - update these values
  SECRET_KEY: "change-me-in-doppler-or-external-secrets"
  # Add other Comet env vars as needed
