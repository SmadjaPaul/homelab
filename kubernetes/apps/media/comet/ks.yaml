---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - namespace.yaml
  - secret.yaml
  - networkpolicy.yaml
  - service-nodeport.yaml
  - helmrelease.yaml

configMapGenerator:
  - name: comet-values
    files:
      - values.yaml

configurations:
  - kustomizeconfig.yaml
