# ============================================================================
# OCI Management Stack - Environment Variables
# ============================================================================
# Copy this file to .env and fill in the values
# NEVER commit .env to git!

# ---------------------------------------------------------------------------
# Cloudflare Tunnel (Zero Trust access)
# ---------------------------------------------------------------------------
# Get this from: Cloudflare Dashboard > Zero Trust > Networks > Tunnels > Create
# Or via Terraform output after enabling the tunnel
CLOUDFLARE_TUNNEL_TOKEN=

# ---------------------------------------------------------------------------
# PostgreSQL (shared database)
# ---------------------------------------------------------------------------
POSTGRES_USER=homelab
POSTGRES_PASSWORD=     # Generate: openssl rand -base64 32
POSTGRES_DB=homelab

# ---------------------------------------------------------------------------
# Authentik (SSO/Identity)
# ---------------------------------------------------------------------------
# Generate: openssl rand -base64 60
AUTHENTIK_SECRET_KEY=

# Optional: Set initial admin password (otherwise auto-generated)
# AUTHENTIK_BOOTSTRAP_PASSWORD=
# AUTHENTIK_BOOTSTRAP_EMAIL=admin@smadja.dev

# ---------------------------------------------------------------------------
# Omni (Talos management)
# ---------------------------------------------------------------------------
OMNI_ACCOUNT_ID=homelab
OMNI_NAME=Homelab Omni
