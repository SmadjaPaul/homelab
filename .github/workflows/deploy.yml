name: Deploy Kubernetes

on:
  push:
    branches:
      - main
    paths:
      - 'kubernetes/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure OCI CLI
        uses: oracle-actions/configure-oci-cli@v1.5.0
        with:
          region: eu-paris-1

      - name: Create kubeconfig
        run: |
          oci ce cluster create-kubeconfig \
            --cluster-id ${{ secrets.OKE_CLUSTER_OCID }} \
            --file ${{ github.workspace }}/kubeconfig \
            --region eu-paris-1 \
            --token-version 2.0.0
        env:
          OKE_CLUSTER_OCID: ${{ secrets.OKE_CLUSTER_OCID }}

      - name: Install kubectl
        run: |
          curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
          chmod +x kubectl
          mv kubectl ${{ github.workspace }}/

      - name: Deploy Bootstrap
        run: |
          export KUBECONFIG=${{ github.workspace }}/kubeconfig
          ${{ github.workspace }}/kubectl apply -f <(kcl run kubernetes/bootstrap/main.k)

      - name: Deploy GitOps
        run: |
          export KUBECONFIG=${{ github.workspace }}/kubeconfig
          ${{ github.workspace }}/kubectl apply -f <(kcl run kubernetes/bootstrap/gitops.k)
