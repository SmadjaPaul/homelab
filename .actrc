# act configuration - Run GitHub Actions locally
# https://github.com/nektos/act

# Use medium-sized runner image (good balance between speed and compatibility)
-P ubuntu-latest=catthehacker/ubuntu:act-latest

# Reuse containers between runs for faster execution
--reuse

# Bind workspace to container (faster than copy)
--bind

# Use local .env file for environment variables
--env-file .env.act

# Use .secrets file for secrets (generated by scripts/act-run.sh)
--secret-file .secrets.act
