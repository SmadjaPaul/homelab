# MachineClass: worker — apply with: omnictl apply -f omni/machine-classes/worker.yaml
#
# Specs (reference): 2 vCPU, 4 GB (DEV) ; 1–2 OCPU, 6–12 GB (CLOUD)
# Use in ClusterTemplate: kind: Workers / machineClass: { name: worker, size: N }

metadata:
  namespace: default
  type: MachineClasses.omni.sidero.dev
  id: worker
spec:
  matchlabels: []
