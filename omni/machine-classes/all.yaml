# All MachineClasses in one file — apply with: omnictl apply -f omni/machine-classes/all.yaml
# Prerequisite: omnictl configured (see omni/README.md)

---
metadata:
  namespace: default
  type: MachineClasses.omni.sidero.dev
  id: control-plane
spec:
  matchlabels: []

---
metadata:
  namespace: default
  type: MachineClasses.omni.sidero.dev
  id: worker
spec:
  matchlabels: []

---
metadata:
  namespace: default
  type: MachineClasses.omni.sidero.dev
  id: gpu-worker
spec:
  matchlabels: []
