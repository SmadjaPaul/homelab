# Example cluster "dev" — apply after MachineClasses (omnictl apply -f omni/machine-classes/all.yaml)
# Then: omnictl apply cluster -f omni/clusters/cluster-dev.yaml
#
# Adjust Talos/Kubernetes versions to match your Omni instance.
# See: https://github.com/siderolabs/contrib/blob/main/examples/omni/infra/cluster-template.yaml

kind: Cluster
name: dev
talos:
  version: v1.8.0
kubernetes:
  version: v1.31.0
---
kind: ControlPlane
machineClass:
  name: control-plane
size: 1
---
kind: Workers
name: workers
machineClass:
  name: worker
size: 1
